This directory is intentionally left blank.

The actual replication scripts (for running experiments and generating summaries)
live in the repository-level `scripts/` directory.  We include this placeholder
because many journal replication packages expect a `scripts` subdirectory within
`replication/`.  Please refer to the top-level `scripts/` directory and
`docs/replication_guide.md` for details.